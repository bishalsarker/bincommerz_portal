export interface Slider {
    id?: string;
    name: string;
    type: string;
}

export interface Slide {
    id?: string;
    imageURL: string;
    title: string;
    description: string;
    buttonText: string;
    buttonUrl: string;
}